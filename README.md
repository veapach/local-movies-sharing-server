# Local Movie Sharing server

**Local Movie Sharing server** — это лёгкий и высокопроизводительный HTTP-сервер на Go для локального стриминга фильмов (включая 4K и более 100 ГБ) на Android TV или другие устройства через VLC.

Сервер **не перекодирует** видео, а отдает файлы напрямую, сохраняя исходное качество и битрейт. Оптимизирован для максимальной скорости без дополнительной нагрузки на CPU.

---

## ⚙️ Возможности

- 🔹 Передача видео без сжатия и перекодирования  
- 🔹 Поддержка `Range`-запросов (seek и перемотка в VLC)  
- 🔹 Минимальная нагрузка на процессор  
- 🔹 Скорость ограничена только сетью и диском  
- 🔹 Тест пропускной способности через `/speedtest`  
- 🔹 Работает "из коробки" — без зависимостей и настройки

---

## 🧩 Установка

Требуется Go 1.20+

```bash
git clone https://github.com/veapach/local-movies-sharing-server.git
cd local-movies-sharing-server
go build -o fileserver fileserver.go
```

▶️ Запуск

```bash
./fileserver -dir /path/to/movies -addr 0.0.0.0:8080
```

После запуска сервер будет доступен по адресу
http://<IP_компьютера>:8080/

Откройте этот адрес в VLC на Android TV:

VLC → Сеть → Ввести URL → http://<IP_компьютера>:8080/

📈 Проверка скорости
Сервер имеет встроенный тест пропускной способности:

```bash
http://<IP>:8080/speedtest
```

Или для конкретного файла:

```bash
http://<IP>:8080/speedtest?file=subfolder/film.mkv
```

В ответ вернётся JSON со скоростью передачи (MB/s).

⚡ Рекомендации для стабильного 4K
Используйте проводное подключение (Gigabit Ethernet)

Храните фильмы на SSD или RAID

Избегайте Wi-Fi при больших файлах

Убедитесь, что Android TV и ПК в одной подсети

📁 Пример
```bash
/movies
 ├── The.Dark.Knight.2008.4K.mkv
 ├── Interstellar.2014.2160p.mkv
 └── Inception.2010.1080p.mp4
```
 
Запуск:

```bash
./fileserver -dir /movies -addr 0.0.0.0:8080
```
После этого откройте:

```bash
http://192.168.1.10:8080/
```
в VLC и выберите нужный фильм.

🧠 Технические детали
Используется стандартный net/http сервер Go

Поддержка HTTP Range реализована через http.ServeContent

Для линейной передачи — буфер 1 MiB (io.CopyBuffer)

Нет шифрования и авторизации для максимальной скорости

Идеально подходит для локальной домашней сети

🛠 Планы
 Автозапуск как systemd-сервис

 Фильтрация только по видеофайлам

 Web-интерфейс с предпросмотром

📜 Лицензия
MIT License © 2025 — Разработано для личного использования.
